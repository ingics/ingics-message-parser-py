from setuptools import setup, find_packages

setup(
        name = "ingics_message_parser",
        version = "0.2.0",
        license = "MIT Licence",
        packages = find_packages(),
        zip_safe=False
)
